# linkedin-Queens-Puzzle-Solver
Solving puzzle game Queens on Linked. Its in initial stages

setup:
	python3 -m venv venv
	. venv/bin/activate && pip install -r requirements.txt
	. venv/bin/activate && playwright install (to download levels)